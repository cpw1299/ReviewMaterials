# CLAUDE.md

本文件提供对 Claude Code (claude.ai/code) 在仓库中工作时的指导说明。


## 语言偏好

- 除非我明确要求，否则请优先使用中文进行回复和交流。
- 代码注释和文档也请使用中文。
- 代码本身（如变量名、函数名）可以保持英文。

## 行为准则

减少编码中常见错误的行为指南。

**权衡：** 这些指南更倾向于谨慎而非速度。对于简单的任务，请自行判断。

### 1. 先想后写

**不要假设。不要隐藏困惑。主动暴露各种权衡。**

在开始实现之前：
- 明确说明你的假设。如果不确定，就提问。
- 如果存在多种理解方式，把它们都列出来——不要默默选择一种。
- 如果有更简单的方法，直接说出来。在有必要时坚持自己的判断。
- 如果有不清楚的地方，停下来。明确指出让你困惑的地方，然后提问。

### 2. 简单至上

**用最少的代码解决问题。不要写任何推测性的代码。**

- 不添加超出需求的功能。
- 不为只使用一次的代码创建抽象。
- 不实现未被要求的"灵活性"或"可配置性"。
- 不为不可能发生的场景进行错误处理。
- 如果你写了200行，而实际上50行就能解决，那就重写。

问问自己："高级工程师会说这过于复杂吗？"如果是，就简化它。

### 3. 精确修改

**只动你必须改的。只收拾你自己造成的烂摊子。**

在编辑现有代码时：
- 不要"改进"邻近的代码、注释或格式。
- 不要重构没有坏的东西。
- 匹配现有风格，即使你可能会用不同的写法。
- 如果你发现了无关的死代码，提出来就好——不要直接删除。

当你的改动造成代码孤立时：
- 删除因你的改动而变得不再使用的导入/变量/函数。
- 不要删除之前就存在的死代码，除非明确要求。

判定标准：每一行被改动的代码，都应能直接追溯到用户的具体要求。

### 4. 目标驱动执行

**定义成功标准。循环执行，直到验证通过。**

将任务转化为可验证的目标：
- "加上验证" → "为无效输入编写测试，然后让测试通过"
- "修复bug" → "先写一个能复现bug的测试，再让它通过"
- "重构X" → "确保重构前后测试都能通过"

对于多步骤任务，先列出一个简明的计划。


## 构建与运行

```bash

```

## 技术栈


## 配置


## 项目结构


## 模块详解


## 注意事项

